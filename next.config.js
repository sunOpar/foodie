// const git = require('git-rev-sync');

module.exports = {
//   generateBuildId: () => {
//     const buildId = `${git.branch()}-${git.short()}`
//     console.log(`buildId: ${buildId}`)
//     return buildId
//   },
}